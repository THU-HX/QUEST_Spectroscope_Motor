# 教程：让大模型帮你执行一个操作（最简可复现步骤）

本教程带你跑通一个 **~50 行** 的最小闭环：从「接入一个模型」到「让它真正执行一次操作」。
读完你能复现 demo，并理解**它为什么能动手**。配套代码：`mini_agent.py`。

---

## 0. 一句话原理

靠 **function calling（工具调用）**，不是靠提示词凭空变出来的：

> 你给模型一份「工具清单」，模型读懂你的话后**不自己动手，而是输出一个结构化结果：要调哪个工具、参数填什么**。
> **真正动手的是你自己的代码**——它拿到这个结果，去调对应的函数。

```
   你说「把3号电机移到5」
        │
        ▼
 ┌───────────────┐   带着「工具清单」一起发过去
 │     你的程序   │ ───────────────────────────►  大模型
 └───────────────┘                                  │ 读懂意图，决定调用哪个工具
        ▲                                            │ 输出: move_motor(motor=3, target=5)
        │   ◄───────────────────────────────────────┘   （这是结构化数据，不是文字）
        │
        ▼
 你的程序拿到 (motor=3,target=5) → 调用你自己的 move_motor() → 真正执行
```

**关键认知**：模型只负责「**决定调哪个工具 + 填参数**」；**执行**永远是你的代码做的。
模型不发工具调用，就什么都不会发生。

---

## 1. 准备

- Python 3.8+（只用标准库，无需 pip 装包）。
- 一个**支持 function calling** 的模型 + OpenAI 兼容端点（一个 `url` + 一个 `key`）。本例用 must-xin，模型 `gpt-5.4`。
- 把 key 放进环境变量（**别写进代码**）：

```bash
export MUSTXIN_API_KEY=sk-你的key
```

---

## 2. 五个步骤（对照 `mini_agent.py`）

**① 定义工具清单**——给模型看的「能力说明书」：工具叫什么、干嘛的、要哪些参数。

```python
TOOLS = [{
    "type": "function",
    "function": {
        "name": "move_motor",
        "description": "把指定电机移动到目标位置（相对中心值）",
        "parameters": {
            "type": "object",
            "properties": {
                "motor":  {"type": "integer", "description": "电机号 1-8"},
                "target": {"type": "number",  "description": "目标位置"},
            },
            "required": ["motor", "target"],
        },
    },
}]
```

**② 写工具真正干活的函数**（demo 里只打印；真项目里在这发电机命令）：

```python
def move_motor(motor, target):
    print(f"   >>> 【真正执行】电机 {motor} 移动到 {target}")
    return f"motor{motor} moved to {target}"

HANDLERS = {"move_motor": move_motor}     # 名字 → 真函数
```

**③ 把「用户的话 + 工具清单」发给模型**：

```python
msg = call_model([{"role": "user", "content": user_say}])
# call_model 里就是 POST /v1/chat/completions，body 带 "tools": TOOLS
```

**④ 看模型返回的是「工具调用」还是「纯文字」**：

```python
calls = msg.get("tool_calls")
if not calls:
    print("模型只回了文字，没有任何操作被执行：", msg.get("content"))
```

**⑤ 真正执行**——把模型填好的参数交给你自己的函数：

```python
for c in calls:
    name = c["function"]["name"]
    args = json.loads(c["function"]["arguments"])   # 模型填的参数是 JSON 字符串
    HANDLERS[name](**args)                           # ← 这一行才真正动手
```

---

## 3. 完整代码

见同目录 `mini_agent.py`（约 50 行，可直接运行）。核心就是上面五步 + 一个 `call_model()`：

```python
def call_model(messages):
    body = json.dumps({"model": MODEL, "messages": messages,
                       "tools": TOOLS, "tool_choice": "auto"}).encode("utf-8")
    req = urllib.request.Request(
        URL.rstrip("/") + "/v1/chat/completions", data=body, method="POST",
        headers={"Authorization": f"Bearer {KEY}",
                 "Content-Type": "application/json",
                 "User-Agent": "Mozilla/5.0"})   # 有些代理会拦默认 UA(403)，给个普通 UA
    with urllib.request.urlopen(req, timeout=60) as r:
        return json.loads(r.read().decode("utf-8"))["choices"][0]["message"]
```

---

## 4. 运行 & 预期输出（实测）

下指令 → 真的"执行"：

```text
$ python mini_agent.py "把3号电机移到5"
模型决定调用：move_motor({'motor': 3, 'target': 5})
   >>> 【真正执行】电机 3 移动到 5
   返回： motor3 moved to 5
```

问个普通问题 → 只回文字、不执行：

```text
$ python mini_agent.py "你好，今天几号"
模型没调用工具，只回了文字： 今天是 ……
→ 没有任何操作被执行。
```

**这两条输出就把原理演示透了**：同一套代码，模型自己判断「该不该动手」。

---

## 5. 从 demo 到真项目（接真电机 + 安全）

1. **把 `move_motor` 换成真命令**——在那个函数里发实际的电机指令即可，上层流程不变。
2. **加「安全闸」**：执行前判断范围，超了就拒绝，别盲发。

   ```python
   def move_motor(motor, target):
       if not (-6 <= target <= 6):          # 范围检查（真项目按各电机配置）
           return f"拒绝：{target} 超出安全范围"
       发真命令(motor, target)
       return "已执行"
   ```

3. **加更多工具**：再往 `TOOLS` 和 `HANDLERS` 各加一条就行——`set_shutter`、`enable_motors`…… 工具越多，能听懂的指令越多。
4. **提示词（prompt）的角色**：加一条 `{"role":"system", ...}` 告诉模型「**何时**该调工具、数值不用它管、**别用文字假装已执行**」。
   注意：prompt 只能**调教一个本来就会工具调用的模型怎么用工具**，不能给不支持的模型**变出**这个能力。

---

## 6. 常见坑

| 现象 | 原因 / 解决 |
| --- | --- |
| 模型说"已执行"但实际没动 | 该模型**不支持 function calling**（弱/小模型常见），只是用文字假装。换支持的模型（如 `gpt-5.4`）。真假区分：**真执行**有结构化 `tool_calls`；假的只有文字。 |
| `403 Forbidden` | 代理前面有 WAF 拦默认 `python-urllib` UA。请求头加普通 `User-Agent`。 |
| key 泄漏 | **别把 key 写进代码**，用环境变量 / 配置文件，并加进 `.gitignore`。 |
| 参数填错（如"打开"给成 closed） | 弱模型理解差。把工具 `description`、参数 `enum` 写清楚，或换强模型。 |
| 真项目里回答是一段段冒出来的 | 那是**流式**（`"stream": true`）。流式时 `tool_calls` 是**分片**传来的，要按 `index` 把 `arguments` 拼起来再 `json.loads`。本 demo 用非流式，更直观。 |

---

## 7. 进阶（可选）

- **让模型用自然语言确认**：执行完把结果作为 `{"role":"tool", "tool_call_id":…, "content":结果}` 再发给模型，它会回一句人话总结。本 demo 省了这步（直接自己打印结果，更简单）。
- **多轮对话**：把历史 `messages` 累积着一起发。
- **多工具协同**：一次回复里模型可能返回多个 `tool_calls`，循环执行即可。

---

**一句话总结**：能不能执行 = 模型**支不支持 function calling**（硬能力）；执行得准不准 = **工具描述 + 提示词**写得好不好（软调教）。
